# Friends leaderboard — see supabase/schema.sql for the one-time setup.
# Both values come from Supabase: Project Settings → API.
# The anon key is meant to be public; row-level security limits what it can do.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
